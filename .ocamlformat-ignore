# Generated by gen.sh from the protos in proto/. Formatting it would make the
# committed output differ from what the plugin emits, so `./gen.sh` would show
# spurious diffs and the tree could silently drift from the .proto files.
lib/proto/gen/**
